export const addBook = (isbn, title, authors, publisher) => {
    //TODO add book to db
}

export const findBookById = (isbn) => {
    //TODO find book by id
}

export const removeBook = (isbn) => {
    //TODO remove book from db
}

export const updateBookTitle = (isbn, title) => {
    //TODO update title of book
}

export const findBooksByAuthor = (author) => {
    //TODO find books by author
}

export const findBooksByPublisher = (publisher) => {
    //TODO find books by publisher
}

export const findBookAuthors = (isbn) => {
    //TODO find authors of book by isbn
}

export const findBookPublishersByAuthor = (author) => {
    //TODO Find publishers of book by author
}

export const removeAuthor = (author) => {
    //TODO remove author from db
}